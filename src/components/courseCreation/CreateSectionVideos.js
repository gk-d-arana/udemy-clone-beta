import React from 'react'
import { connect } from 'react-redux'

export const CreateSectionVideos = (props) => {
    return (
        <div>
            CreateSectionVideos
        </div>
    )
}

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(CreateSectionVideos)
