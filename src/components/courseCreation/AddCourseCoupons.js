import React from 'react'
import { connect } from 'react-redux'

export const AddCourseCoupons = (props) => {
    return (
        <div>
            AddCourseCoupons
        </div>
    )
}

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(AddCourseCoupons)
