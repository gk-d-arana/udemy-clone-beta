import React from 'react'
import { connect } from 'react-redux'

export const CourseCreationModals = (props) => {
    return (
        <div>
            CourseCreationModals
        </div>
    )
}

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(CourseCreationModals)
