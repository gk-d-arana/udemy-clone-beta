import React from 'react'
import { connect } from 'react-redux'

export const InstructorReviews = (props) => {
    return (
        <div>
            InstructorReviews
        </div>
    )
}

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(InstructorReviews)
